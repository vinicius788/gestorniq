# Supabase (auto-configured by Lovable Cloud)
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SENTRY_DSN=""

# Clerk (frontend only)
VITE_CLERK_PUBLISHABLE_KEY="pk_test_..."
# Optional alias if your setup uses NEXT_PUBLIC_* conventions
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
# Optional: your Clerk frontend API URL / issuer (e.g. https://key-bug-33.clerk.accounts.dev)
VITE_CLERK_FRONTEND_API_URL=""
# Clerk JWT template name used to mint Supabase-compatible tokens
VITE_CLERK_SUPABASE_JWT_TEMPLATE="supabase"
# Optional override for Supabase signInWithIdToken provider/issuer
VITE_CLERK_SUPABASE_PROVIDER=""
# Set to "true" to use Clerk auth without requiring Supabase token exchange
VITE_CLERK_ONLY_MODE="false"

# Runtime mode for edge functions.
# Use "production" in deployed environments.
APP_ENV="development"

# Public app URL used by checkout/portal redirects and CORS allowlist base.
# Required when APP_ENV is production/staging/release.
APP_URL=""

# Optional local/dev fallback for redirect generation.
# In production APP_URL is required and SITE_URL fallback is disabled.
SITE_URL=""

# Optional CORS allowlist for edge functions (comma-separated origins).
# Defaults to APP_URL/SITE_URL when omitted.
# Supports exact origins and wildcard patterns like https://*.vercel.app.
# Recommended:
#   production -> https://app.gestorniq.com
#   staging    -> https://staging.gestorniq.com,https://*.vercel.app
CORS_ALLOWED_ORIGINS=""

# Optional local override for permissive CORS during development only.
CORS_ALLOW_ALL_LOCAL="false"

# The following secrets must be configured in Supabase project secrets:
# STRIPE_SECRET_KEY                  - Stripe secret key (sk_live_... or sk_test_...)
# STRIPE_WEBHOOK_SECRET              - Stripe webhook signing secret (whsec_...)
# STRIPE_PRICE_STANDARD_ANNUAL       - Stripe Price ID for the Standard annual plan (price_...)
# APP_URL                            - Public app URL used for checkout/portal redirects (e.g. https://app.gestorniq.com)
# SUPABASE_SERVICE_ROLE_KEY          - Supabase service role key (server only)
# CLERK_SECRET_KEY                   - Clerk secret key (server only)
# STRIPE_CONNECTIONS_ENCRYPTION_KEY  - Base64 encoded 32-byte key for Stripe connection encryption at rest (required in production)
